
export const metadata = {
  title: "Speech To Text to MQTT",
  description: "Convert speech to text and publish it to mqtt topic",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
